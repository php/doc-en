<?xml version="1.0" encoding="big5"?>
<!-- $Revision: 1.60 $ -->
 <chapter id="language.variables">
  <title>各類變數</title>
  
  <sect1 id="language.variables.basics">
   <title>基本知識</title>

   <simpara>
    所有 PHP 的變數都是以 "$" 為開端，後加變數名所組成的。變數名稱是有大小寫之分的。
   </simpara>

   <para>
    PHP 的變數名稱和其他標籤有著相同的規則。一個有效的變數是由英文字母或底線開始，跟著是任意長短的字母、數字或底線。按正規運算式，它可以表達為：'[a-zA-Z_\x7f-\xff][a-zA-Z0-9_\x7f-\xff]*'
   </para>
   
   <note>
    <simpara>
    字母為 a-z，A-Z，而 ASCII 字符則從 127 至 255〈0x7f-0xff〉。 
    </simpara>
   </note>

   <para>
    <informalexample>
     <programlisting role="php"> 
<![CDATA[
<?php
$var = "Bob";
$Var = "Joe";
echo "$var, $Var";      // outputs "Bob, Joe"

$4site = 'not yet';     // invalid; starts with a number
$_4site = 'not yet';    // valid; starts with an underscore
$t銛te = 'mansikka';    // valid; '�' is ASCII 228.
?>
]]>
     </programlisting>
    </informalexample>
   </para>

   <para>
    PHP 3 中，變數總是由值指派。也就是說，當您將一個運算式的值指派給一個變數時，整個原始運算式的值將會複製到目的變數。例如，當將一個變數的值指派給另一個變數之後，更改當中的任何一個變數的值並不會影響到另一個變數的值。想要知道更多這類的指派，請參閱<link linkend="language.expressions">運算式</link>這一章。
   </para>
   <para>
    PHP 4 則提供了另一種指派變數值的方法：<link linkend="language.references">傳址指派</link>，即新變數只是參照原來變數中的值，或說新的變數成為原來變數的別名。此時，更改兩個變數中任何一個的值都會同時更改另一個的值。由於沒有執行實際的複製工作，指派的操作也因此更加快捷。不過，只有在緊密的迴圈或指派大量的<link linkend="language.types.array">陣列</link>或<link linkend="language.types.object">物件</link>時才會察覺速度的提升。
   </para>
   <para>
    使用傳址指派，只須在被參照的原變數前加上一個 (&amp;) 的符號。例如，下列程式碼在使用傳址指派後輸出 'My name is Bob' 兩次。
    
    <informalexample>
     <programlisting role="php">
<![CDATA[
<?php
$foo = 'Bob';              // Assign the value 'Bob' to $foo
$bar = &$foo;              // Reference $foo via $bar.
$bar = "My name is $bar";  // Alter $bar...
echo $bar;
echo $foo;                 // $foo is altered too.
?>
]]>
     </programlisting>
    </informalexample>
   </para>

   <para>
    需要注意的是只有真正命名的變數才可以用來進行傳址指派。
    <informalexample>
     <programlisting role="php">
<![CDATA[
<?php
$foo = 25;
$bar = &$foo;      // This is a valid assignment.
$bar = &(24 * 7);  // Invalid; references an unnamed expression.

function test()
{
   return 25;
}

$bar = &test();    // Invalid.
?>
]]>
     </programlisting>
    </informalexample>
   </para>
  </sect1>

  <sect1 id="language.variables.predefined">
   <title>預設的變數</title>
   
   <simpara>
    PHP 為它執行的程式提供了大量的預設變數。然而，由于有許多變數因依賴其所執行的伺服器種類、版本、安裝等情況而定，因此難以有完整的被記載下來。更有一些變數在<link linkend="features.commandline">指令行</link>的模式運行時是不能使用的。有關這些變數的列表，請參考<link linkend="reserved.variables">保留的先前定義變數</link>一節。
   </simpara>

   <warning>
    <simpara>
    PHP 4.2.0 以後的版本，<link linkend="ini.register-globals">register_globals</link> 指令的預設值是 <emphasis>off</emphasis>。這是一個重大的改變。將 register_globals 的值預設為 <emphasis>off</emphasis> 會影響到預設變數在全域範圍內的有效性。例如，要得到 <varname>DOCUMENT_ROOT</varname> 的值，應使用 <varname>$_SERVER['DOCUMENT_ROOT']</varname> 而不是 <varname>$DOCUMENT_ROOT</varname>、使用 <varname>$_GET['id']</varname> 以取得 URL <literal>http://www.example.com/test.php?id=3</literal> 中 id 的值，而不是用 <varname>$id</varname>、或使用 <varname>$_ENV['HOME']</varname> 來取代 <varname>$HOME</varname>。
    </simpara>
    <simpara>
    更多與此更改相關的資料可在 <link linkend="ini.register-globals">register_globals</link> 的設置一節、<link linkend="security.registerglobals">使用 Register Globals</link> 中的安全章，或在 <ulink url="&url.php.release4.1.0;">4.1.0</ulink> 及 <ulink url="&url.php.release4.2.0;">4.2.0</ulink> 的發行通告中取得。
    </simpara>
    <simpara>
    我們建議您使用 PHP 提供的保留預設變數，如 <link linkend="language.variables.superglobals">superglobal 陳列</link>。
    </simpara>
   </warning>

   <simpara>
    自 4.1.0 版本起，PHP 額外提供了一套預設陣列，當中包含了來自伺服器〈如適用〉、環境、及使用者輸入的變數。這些新陳列比較特別，因此它們會自動地設為全域，即可以在所有範圍內使用。正因如此，它們也被稱為 'autoglobals' 或 'superglobals'。〈PHP 尚未有使用者自行定義 superglobals 的功能。〉我們將在下面列出 superglobals，但要知道它們的內容和 PHP 預設變數的深入討論，請參考<link linkend="reserved.variables">保留的先前定義變數</link>一節。此外，您也會留意舊有的預設變數 (<varname>$HTTP_*_VARS</varname>) 仍然存在。
    
    &avail.register-long-arrays;
   </simpara>
   
   <note>
    <title>可變變數</title>
    <para>
    Superglobals 不能當作<link linkend="language.variables.variable">可變變數</link>來使用。
    </para>
   </note>

   <para>
    如果某些 <link linkend="ini.variables-order">variables_order</link> 中的變數沒有被設定，它們相對的 PHP 預設陳列也將會是空的。
   </para>

   <variablelist id="language.variables.superglobals">
    <title>PHP 的 Superglobals</title>
    <varlistentry>
     <term><link linkend="reserved.variables.globals">$GLOBALS</link></term>
     <listitem>
      <simpara>
      包含一個指向程式全域範圍內可用變數的索引。此陳列的索引鍵為全域變數的名稱。<varname>$GLOBALS</varname> 自 PHP 3 開始就已存在。
      </simpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><link linkend="reserved.variables.server">$_SERVER</link></term>
     <listitem>
      <simpara>
      由網頁伺服器設定的變數或目前的程式所執行的環境產生的變數。其性質類似舊有的 <varname>$HTTP_SERVER_VARS</varname> 陳列，雖然目前還可以使用，但不被建議。
      </simpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><link linkend="reserved.variables.get">$_GET</link></term>
     <listitem>
      <simpara>
      通過 HTTP GET 提供給程式的變數。其性質類似舊有的 <varname>$HTTP_GET_VARS</varname> 陳列，雖然目前還可以使用，但不被建議。
      </simpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><link linkend="reserved.variables.post">$_POST</link></term>
     <listitem>
      <simpara>
      通過 HTTP POST 提供給程式的變數。其性質類似舊有的 <varname>$HTTP_POST_VARS</varname>陳列，雖然目前還可以使用，但不被建議。
      </simpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><link linkend="reserved.variables.cookies">$_COOKIE</link></term>
     <listitem>
      <simpara>
      通過 HTTP cookies 提供給程式的變數。其性質類似舊有的 <varname>$HTTP_COOKIE_VARS</varname>陳列，雖然目前還可以使用，但不被建議。
      </simpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><link linkend="reserved.variables.files">$_FILES</link></term>
     <listitem>
      <simpara>
      通過 HTTP POST 檔案上傳提供給程式的變數。其性質類似舊有的 <varname>$HTTP_POST_FILES</varname>陳列，雖然目前還可以使用，但不被建議。詳情請參閱 <link linkend="features.file-upload.post-method">POST 上傳方式</link>。
      </simpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><link linkend="reserved.variables.environment">$_ENV</link></term>
     <listitem>
      <simpara>
      執行環境提供給程式的變數。其性質類似舊有的 <varname>$HTTP_ENV_VARS</varname>陳列，雖然目前還可以使用，但不被建議。
      </simpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><link linkend="reserved.variables.request">$_REQUEST</link></term>
     <listitem>
      <simpara>
       通過任何使用者輸入途徑而提供給程式的變數，也正因如此不值得信任。此陳列中變數的存在與否及次序是決定於 <link linkend="ini.variables-order">variables_order</link> 的設定。PHP 4.1.0 之前的版本並沒有類似此陳列的變數。請同時參閱 <function>import_request_variables</function>。
      </simpara>
      <note>
       <simpara>
        在<link linkend="features.commandline">指令行</link>模式下執行時，它並<emphasis>不</emphasis>包含 <varname>argv</varname> 和 <varname>argc</varname> 項目。此兩項目只在 <varname>$_SERVER</varname> 陳列中出現。
       </simpara>
      </note> 
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><link linkend="reserved.variables.session">$_SESSION</link></term>
     <listitem>
      <simpara>
       目前和程式注冊了的 session 變數。其性質類似舊有的 <varname>$HTTP_SESSION_VARS</varname>陳列，雖然目前還可以使用，但不被建議。詳情請參閱 <link linkend="ref.session">Session 處理函數</link>一節。
      </simpara>
     </listitem>
    </varlistentry>
   </variablelist>

  </sect1>


  <sect1 id="language.variables.scope">
   <title>變數範圍</title>

   <simpara>
    變數的範圍只是在其所定義的空間內存在。在大部份的情況下，PHP 的變數只有一個單一的範圍。這單一的範圍也包含了以 include 和 require 方式引入的檔案。例如：
   </simpara>
   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
$a = 1;
include "b.inc";
?>
]]>
    </programlisting>
   </informalexample>
   <simpara>
    例子中，變數 <varname>$a</varname> 也存在于被包含入的程式 <filename>b.inc</filename> 中。但是在使用者自訂的函數中，一個區域性的函數範圍將被引用。任何在函數內使用的變數在預設的情況下只局限於該函數的範圍內。例如：
   </simpara>
    
   <informalexample>
    <programlisting role="php"> 
<![CDATA[
<?php
$a = 1; /* global scope */ 

function Test()
{ 
    echo $a; /* reference to local scope variable */ 
} 

Test();
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    這個程式不會輸出任何東西，因為 echo 述句使用了本區域版本的 <varname>$a</varname> 變數，而該本區域變數並未曾被分配一個值。你會發現這和 C 語言的做法不同，因為 C 的全域變數是自動的提供給各函數，除非在本域定義中指明撤銷。此做法可能會引致一些問題，如使用者不慎地更改了全域變數的值。在 PHP 中，全域變數在本域使用前必須先宣佈為全域。例子：
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
$a = 1;
$b = 2;

function Sum()
{
    global $a, $b;

    $b = $a + $b;
} 

Sum();
echo $b;
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    上述例子將會輸出 &quot;3&quot;。在函數內宣佈 <varname>$a</varname> 及 <varname>$b</varname> 為全域後，所有涉及該兩個變數的使用將自動指向全域的版本。PHP 沒有限定一個函數可以使用的全域變數的數量。
   </simpara>

   <simpara>
    第二種存取全域範圍中的變數方法是使用一個由 PHP 特別定義的陳列：<varname>$GLOBALS</varname>。前面的例子可以重寫為：
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
$a = 1;
$b = 2;

function Sum()
{
    $GLOBALS["b"] = $GLOBALS["a"] + $GLOBALS["b"];
} 

Sum();
echo $b;
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    <varname>$GLOBALS</varname> 是一個關聯陳列 (associative array)，全域變數的名即為索引鍵，而該變數的內容即為陳列的值。有否留意到 <varname>$GLOBALS</varname> 在任何一個範圍都出現？那是因為 $GLOBALS 是一個<link linkend="language.variables.superglobals">superglobal</link>。這是一個示範 superglobals 強大功能的例子：
   </simpara>
   <para>
   
   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
function test_global()
{
    // Most predefined variables aren't "super" and require 
    // 'global' to be available to the functions local scope.
    global $HTTP_POST_VARS;
    
    print $HTTP_POST_VARS['name'];
    
    // Superglobals are available in any scope and do 
    // not require 'global'.  Superglobals are available 
    // as of PHP 4.1.0
    print $_POST['name'];
}
?>
]]>
     </programlisting>
    </informalexample>
   </para>
   
   <simpara>
    變數範圍的另一個重要功能為<emphasis>靜態</emphasis>變數。靜態變數只在本域函數範圍內存在，但是當程式執行離開此範圍時，它並不會喪失它的值。看看下面的例子：
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
function Test ()
{
    $a = 0;
    echo $a;
    $a++;
}
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    此函數沒有什麼用處，因每一次執行它時，它將 <varname>$a</varname> 設為 <literal>0</literal> 然後列印出 &quot;0&quot;。<varname>$a</varname>++ 增加了 <varname>$a</varname> 的值，但並沒有實際用途因為一旦離開了該函數，變數 <varname>$a</varname> 也隨之消失。要設計一個有用的、不會丟失當前計數的計數函數，我們可以將 <varname>$a</varname>宣佈為靜態：
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
function Test()
{
    static $a = 0;
    echo $a;
    $a++;
}
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    現在，每當函數 Test() 被呼叫時，它會列印出 <varname>$a</varname> 的值，然後加上一。
   </simpara>

   <simpara>
    靜態變數也提供一種處理遞迴函數的方法。遞迴函數是一種呼叫自己的函數。編寫遞迴函數時必須留意，因為若編寫錯誤，它有可能會無定限地遞迴。您必須確定足夠的方式來終止遞迴。下列簡單的函數將遞迴地數到 10，利用靜態變數 <varname>$count</varname> 來斷定什麼時候停止： 
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
function Test()
{
    static $count = 0;

    $count++;
    echo $count;
    if ($count < 10) {
        Test ();
    }
    $count--;
}
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    驅動 <literal>PHP 4</literal> 的 Zend Engine 1 是以參照的方式來實現 <literal>static</literal> 和 <literal>global</literal> 的。例如，一個真正的全域變數使用 <literal>global</literal> 方式引進一個函數範圍時實際上就是建立了一個全域變數的參照。這將導致一些意想不到的行為，正如下列例子所述：
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
function test_global_ref() {
    global $obj;
    $obj = &new stdclass;
}

function test_global_noref() {
    global $obj;
    $obj = new stdclass;
}

test_global_ref();
var_dump($obj);
test_global_noref();
var_dump($obj);
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    執行這個例子將會導致下列的輸出：
   </simpara>

   <screen>
NULL
object(stdClass)(0) {
}
   </screen>

   <simpara>
    <literal>static</literal> 陳述式也會導致同樣的輸出。參照並沒有被靜態地儲存：
   </simpara>
   
   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
function &get_instance_ref() {
    static $obj;

    echo "Static object: ";
    var_dump($obj);
    if (!isset($obj)) {
        // Assign a reference to the static variable
        $obj = &new stdclass;
    }
    $obj->property++;
    return $obj;
}

function &get_instance_noref() {
    static $obj;

    echo "Static object: ";
    var_dump($obj);
    if (!isset($obj)) {
        // Assign the object to the static variable
        $obj = new stdclass;
    }
    $obj->property++;
    return $obj;
}

$obj1 = get_instance_ref();
$still_obj1 = get_instance_ref();
echo "\n";
$obj2 = get_instance_noref();
$still_obj2 = get_instance_noref();
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
   執行此例子將導致下列的輸出：
   </simpara>

   <screen>
Static object: NULL
Static object: NULL

Static object: NULL
Static object: object(stdClass)(1) {
  ["property"]=>
  int(1)
}
   </screen>

   <simpara>
    上述例子示範了在指派一個參照給予一個靜態變數後，當您第二次呼叫 <literal>&amp;get_instance_ref()</literal> 函數時，它是<emphasis>不會</emphasis>記住之前的值的。
   </simpara>


  </sect1>

  <sect1 id="language.variables.variable">
   <title>可變變數</title>

   <simpara>
    有時候，可以使用可變變數會令到工作更加方便。也就是就，一個變數的名可以被動態的設定和使用。一個普通的變數是以下列的方法設定：
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
$a = "hello";
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    一個可變變數是以另一個變數的值作為它的變數名。在上述例子中，只要加兩個 $ 在 <emphasis>hello</emphasis> 的前面就可把它當作是一個變數的名來使用了。即：
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
$$a = "world";
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    這時，有兩個變數被定義然後儲存在 PHP 的符號表中：含有 "hello" 的 <varname>$a</varname> 及含有 "world" 的 <varname>$hello</varname>。所以，此敘述：
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
echo "$a ${$a}";
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    與下列這句有著同樣的輸出：
   </simpara>

   <informalexample>
    <programlisting role="php">
<![CDATA[
<?php
echo "$a $hello";
?>
]]>
    </programlisting>
   </informalexample>

   <simpara>
    即，它們都輸出：<computeroutput>hello world</computeroutput>。
   </simpara>

   <simpara>
    為了要在陳列中使用可變變數，您必須首先解決一個模糊的問題。比方說，如果您使用 <varname>$$a[1]</varname>，那剖析器必須知道您是想當 <varname>$a[1]</varname> 是變數來使用，還是當 <varname>$$a</varname> 是變數，然後指向那個變數的第 [1] 個索引。解決上述模糊問題的語法是：第一個情況使用 <varname>${$a[1]}</varname>、第二個情況使用 <varname>${$a}[1]</varname>。
   </simpara>
   
   <warning>
    <simpara>
     必須留意的是，可變變數是不能使用於 PHP 的 <link linkend="language.variables.superglobals">Superglobal 陳列</link>。這表示您不能使用 <varname>${$_GET}</varname> 這樣的敘述。如果您真的想使用 superglobals 和舊的 <varname>HTTP_*_VARS</varname>，您可以嘗試以<link linkend="language.references">參照</link>的方式使用。
    </simpara>
   </warning>
  
  </sect1>

  <sect1 id="language.variables.external">
   <title>來自 PHP 以外的變數</title>
   
   <sect2 id="language.variables.external.form">
    <title>HTML 表單 (GET 和POST)</title>

    <simpara>
     當一份表單傳送給 PHP 程式時，表單內的資訊將自動提供給該程式。我們有很多方法存取這些資訊，範例：
    </simpara>

    <para>
     <example>
      <title>一個簡單的 HTML 表單</title>
      <programlisting role="html">
<![CDATA[
<form action="foo.php" method="post">
    Name:  <input type="text" name="username"><br>
    Email: <input type="text" name="email"><br>
    <input type="submit" name="submit" value="Submit me!">
</form>
]]>
      </programlisting>
     </example>
    </para>

    <para>
     根據您的安裝方式和個人偏好，PHP 提供多種方式存取 HTML 表單中的資料。一些例子如：
    </para>
    
    <para>
     <example>
      <title>存取一個以 POST 方式傳送的簡單 HTML 表單</title>
      <programlisting role="html">
<![CDATA[
<?php 
// Available since PHP 4.1.0

   print $_POST['username'];
   print $_REQUEST['username'];

   import_request_variables('p', 'p_');
   print $p_username;

// Available since PHP 3. As of PHP 5.0.0, these long predefined
// variables can be disabled with the register_long_arrays directive.

   print $HTTP_POST_VARS['username'];

// Available if the PHP directive register_globals = on.  As of 
// PHP 4.2.0 the default value of register_globals = off.
// Using/relying on this method is not preferred.

   print $username;
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     使用 GET 方式傳送的表單用法類似，只不過使用合適的先前定義的函數。GET 也可使用在查詢字串 (QUERY_STRING) 〈URL 中問號 '?' 後面的資訊〉。例如，<literal>http://www.example.com/test.php?id=3</literal> 含有以 <varname>$_GET['id']</varname> 方式存取的 GET 資料。請參考 <link linkend="reserved.variables.request">$_REQUEST</link> 和 <function>import_request_variables()</function>。
    </para>

    <note>
     <para>
      <link linkend="language.variables.superglobals">Superglobal 陳列</link>如 <varname>$_POST</varname> 及 <varname>$_GET</varname> 在 PHP 版本 4.1.0 以上才開始提供。
     </para>
    </note>

    <para>
     上面提到，在 PHP 4.2.0 之前的版本中，<link linkend="ini.register-globals">register_globals</link> 的預設值是 <emphasis>on</emphasis>。而在 PHP 3 中，它永遠是 on 的。我們建議所有使用者不要依賴此指令，並在編寫時將它當作是 <emphasis>off</emphasis> 來設計您的程式。
    </para>

    <note>
     <para>
      <link linkend="ini.magic-quotes-gpc">magic_quotes_gpc</link> 組態指令會影響到 Get，Post 和 Cookie 的值。如果開啟此指令，(It's "PHP!") 的值將變成 (It\'s \"PHP!\")。此跳出工作在將字串插入資料庫前是必須要做的。請參考 <function>addslashes</function>，<function>stripslashes</function> 和 <link linkend="ini.magic-quotes-sybase">magic_quotes_sybase</link>。
     </para>
    </note>
    
    <simpara>
     PHP 也懂得處理陳列式的表單變數〈請參觀相關的 <link linkend="faq.html">FAQ</link>〉。您可以將相關的變數歸類，或用此功能來取回多元選擇輸入表單的值。在下列範例中，我們將表單張貼到它自己，並在傳送後把資料顯示出來：

    </simpara>

    <para>
     <example>
      <title>更複雜的表單變數</title>
      <programlisting role="php">
<![CDATA[
<?php
if ($HTTP_POST_VARS['action'] == 'submitted') {
    print '<pre>';

    print_r($HTTP_POST_VARS);
    print '<a href="'. $HTTP_SERVER_VARS['PHP_SELF'] .'">Please try again</a>';

    print '</pre>';
} else {
?>
<form action="<?php echo $HTTP_SERVER_VARS['PHP_SELF']; ?>" method="post">
    Name:  <input type="text" name="personal[name]"><br>
    Email: <input type="text" name="personal[email]"><br>
    Beer: <br>
    <select multiple name="beer[]">
        <option value="warthog">Warthog</option>
        <option value="guinness">Guinness</option>
        <option value="stuttgarter">Stuttgarter Schwabenbr酳</option>
    </select><br>
    <input type="hidden" name="action" value="submitted">
    <input type="submit" name="submit" value="submit me!">
</form>
<?php
}
?>
]]>
      </programlisting>
     </example>
    </para>

    <para>
     PHP 3 的陳列表單變數只可以使用一維陳列，而 PHP 4 則沒有此限制。
    </para>

    <sect3 id="language.variables.external.form.submit">
     <title>IMAGE SUBMIT 變數名稱</title>

     <simpara>
      在傳送表單時，我們可以使用下列標籤將標準的提交鍵以圖像來取代：
     </simpara>

     <informalexample>
      <programlisting role="html">
<![CDATA[
<input type="image" src="image.gif" name="sub">
]]>
      </programlisting>
     </informalexample>

     <simpara>
      當使用者在圖像的任何一處按一下，伴隨的表單就會傳送到伺服器，隨其而來還附加了兩個變數，sub_x 和 sub_y。這包含了使用者按在圖像的座標。有經驗的使用者或許知道由瀏覽器傳送來的變數名稱應該包含句號，而不是底線，但 PHP 自動地將句號轉換成為底線。
     </simpara>
    </sect3>

   </sect2>

   <sect2 id="language.variables.external.cookies">
    <title>HTTP Cookies</title>

    <simpara>
     PHP 絕對支援 <ulink
     url="&spec.cookies;">Netscape規格</ulink>的 HTTP cookies。Cookies 是用來將資料儲存在用戶端的瀏覽器中，以便日後追蹤或識別返回的使用者。您可以用 <function>setcookie()</function> 函數來設定 cookies。Cookies 是 HTTP header 的一部份，因此 SetCookie 函數一定要在任何資料傳送去瀏覽器前呼叫。這和 <function>header()</function> 函數有著相同的限定。Cookie 資料可以在 <varname>$_COOKIE</varname>，<varname>$HTTP_COOKIE_VARS</varname> 及 <varname>$_REQUEST</varname> 陳列中供使用。詳情和範例請參考手冊的 <function>setcookie()</function> 部份。
    </simpara>

    <simpara>
     若您要將多個值指派給一個 cookie 變數，那您就要使用陳列來指派了。範例：
    </simpara>

    <informalexample>
     <programlisting role="php">
<![CDATA[
<?php
  setcookie("MyCookie[foo]", "Testing 1", time()+3600);
  setcookie("MyCookie[bar]", "Testing 2", time()+3600);
?>
]]>
     </programlisting>
    </informalexample>
    
    <simpara>
     這將會建立兩個個別的 cookies，儘管現在 MyCookie在您的程式中是一個單元陳列。如果您要以一個 cookie 建立多個值，那您可先對那些值使用 <function>serialize()</function> 或 <function>explode()</function> 函數。
    </simpara>

    <simpara>
     有一點必須留意的是，一個 cookie 將會替代瀏覽器中另一個同名的 cookie，除非該 cookie 的路徑或網域名稱不同。所以，在設計購物車程式時，您最好保持一個計數器，並將此數據傳遞下去，即：
    </simpara>

    <example>
     <title>一個 <function>setcookie()</function> 的範例</title>
     <programlisting role="php">
<![CDATA[
<?php
$count++;
setcookie("count", $count, time()+3600);
setcookie("Cart[$count]", $item, time()+3600);
?>
]]>
     </programlisting>
    </example>

   </sect2>

   <sect2 id="language.variables.external.dot-in-names">
    <title>在變數名字中的句號 (dots)</title>

    <para>
     一般上，PHP 並不會更改傳入程式的變數名稱。然而，您必須留意句號 (.) 並不是一個有效的 PHP 變數名稱字元，正如：

     <programlisting role="php">
<![CDATA[
<?php
$varname.ext;  /* invalid variable name */
?>
]]>
     </programlisting>
     這時，剖析器見到的只是一個叫做 <varname>$varname</varname> 的變數，跟隨著字串連結運算子 (.)，再來是一個無引號的字串：ext。顯然地，這並不會帶來您想要的結果。
    </para>

    <para>
     正因如此，PHP 會自動的將傳入的變數名稱中的句號 (.) 改為底線 (_)。
    </para>

   </sect2>

   <sect2 id="language.variables.determining-type-of">
    <title>判定變數的類型</title>

    <para>
     由於 PHP 會自行判斷變數的類型，並按需要轉變它們的類型，因此有時也難以明顯地判定一個變數在某個時刻的類型。有鑑於此，PHP 提供了幾個判定變數類型的函數：<function>gettype()</function>、<function>is_array()</function>、<function>is_float()</function>、<function>is_int()</function>、<function>is_object()</function> 及 <function>is_string()</function>。請參閱<link linkend="language.types">類型</link>一章。
    </para>
   </sect2>

  </sect1>
     
 </chapter>
 
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->